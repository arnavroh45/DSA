Approach:  inorder traversal karenge and jab bhi hum ek value encounter karenge counter ko ++ kardenge cox inorder traversal ascending order mai hota hai
int count = 0;
    int ans;
    void inorder(TreeNode* root, int k)
    {
        if(!root || count>k)
            return;
        inorder(root->left, k);
        count++;
        if(count==k)
        {
            ans = root->val;
            return;
        }
        inorder(root->right, k);
    }
    int kthSmallest(TreeNode* root, int k) {
        inorder(root, k);
        return ans;
    }
